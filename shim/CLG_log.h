/* SPDX-License-Identifier: GPL-2.0-or-later
 *
 * Stub for CLG_log.h. The CLOG_* macros are defined in clog_stubs.h, which
 * is force-included into every translation unit. This file exists purely
 * so explicit `#include "CLG_log.h"` resolves; the macros it would have
 * declared are already in scope.
 */
#pragma once
